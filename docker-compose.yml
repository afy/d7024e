services:
  node:
    build: .
    deploy:
      replicas: 5
    environment:
      BOOTSTRAP_PORT: ${PORT}
      PORT: ${PORT}
    depends_on:
      - bootstrap-node
    ports:
      - ${PORT}
    volumes:
      - .:/app
  bootstrap-node:
    build: .
    ports:
      - ${PORT}:${PORT}
    environment:
      BOOTSTRAP_PORT: ${PORT}
      PORT: ${PORT}

